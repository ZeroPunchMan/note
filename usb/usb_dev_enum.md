# 设备枚举过程

```
1.检测到设备插入,向地址0获取设备描述符前8字节(包含EP0包最大字节数), 同一时间只能枚举一个设备(地址0公用).
2.SET_ADDRESS给设备分配一个新地址.
3.获取完整的设备描述符(GET_DESCRIPTOR).
4.获取完整的配置描述符(GET_DESCRIPTOR).
5.获取字符描述符(GET_DESCRIPTOR).
6.其他设备类型专用描述符...
```